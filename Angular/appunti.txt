alternativve a Bootstrap:
Material: https://m2.material.io/
Tailwind: https://tailwindcss.com/

Per vedere 
l'ingombro di ogni div della vosta pagina html, scrivete questo nella console:

[].forEach.call($$("*"),function(a){a.style.outline="1px solid #"+(~~(Math.random()*(1<<24))).toString(16)})

elenco e spiegazione eventi:  https://developer.mozilla.org/en-US/docs/Web/Events

                            : https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement


esempio per la gestione della retituzione della promise a null: [if (x != null) {...]

import { Component } from '@angular/core';
import { Hotel } from '../hotel.model';
import { HotelService } from '../hotel.service';

@Component({
  selector: 'app-hotel-list',
  templateUrl: './hotel-list.component.html',
  styleUrls: ['./hotel-list.component.css'],
})
export class HotelListComponent {
  hotelList: Hotel[] = [];

  constructor(private hotelService: HotelService) {
    this.hotelService.getHotelList().then((x) => {
      if (x != null) {
        this.hotelList = x;
      }
    });
  }
}

N.B bisogna continuare a gestire il null nel jhtml del component 

<div *ngIf="hotel != null">
  <img [src]="hotel.image">
  <h3>{{hotel.name}} ({{hotel.stars}} stelle)</h3>
  <p>Indirizzo: {{hotel.address}}</p>
</div>